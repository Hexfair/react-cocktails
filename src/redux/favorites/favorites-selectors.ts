import { RootState } from './../store';
//=========================================================================================================================

export const selectorFavoritesList = (state: RootState) => state.favorites.favoritesList